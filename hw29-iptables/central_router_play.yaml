---
- name: Set up centralRouter
  hosts: centralRouter
  become: yes
  tasks:
    - name: install nmap
      yum:
        name: nmap
        state: present

    - name: install nginx
      yum:
        name: nginx
        state: present

    - name: start nginx
      service:
        name: nginx
        state: started
        enabled: true
